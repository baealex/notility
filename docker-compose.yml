version: "2.0"

services:
  app:
    build:
      context: ./server
      dockerfile: ./Dockerfile
    # volumes:
    #   - ./server/src:/app
    # command:
    #   - "dev"
    ports:
      - 3000:3000